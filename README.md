# TorLocalProxy 0.0.1

Кросс-платформенный клиент луковой сети с поддержкой мостов: на десктопе поднимает
локальный прокси (SOCKS5 + HTTP), на мобильных работает как VPN. То, что делает Tor
Browser при подключении, но без браузера — только доступ к сети.

Язык — **Go** (правило 11, по согласованию: приоритетный C# уступает потому, что весь
экосистем pluggable transports написан на Go и на iOS подключается только как Go-код).

## 🗂 Структура проекта

```
TorLocalProxy/
├─ PLAN.md                     пошаговый план сборки и тестирования (для Code)
├─ CONTRIBUTING.md             команды сборки, тестов и линтера
├─ LICENSE                     BSD 3-Clause
├─ .gitlab-ci.yml              быстрые проверки и кросс-сборка (основной репозиторий)
├─ .github/workflows/ci.yml    тесты на Linux, Windows и macOS + родные бинарники
├─ Source/                     исходный код
│  ├─ ЛОГИКА.md                карта логики: как всё устроено и почему
│  ├─ go.work                  рабочее пространство: core + cmd/torproxy
│  ├─ core/                    ядро; ничего не знает о UI и платформах
│  │  ├─ bridges/             разбор строк мостов
│  │  ├─ torconf/             генерация torrc
│  │  ├─ control/             control-протокол, события bootstrap
│  │  └─ torrun/              жизненный цикл tor: интерфейс + external
│  ├─ cmd/torproxy/            десктопная команда
│  └─ Prototype-Python/        рабочий прототип — источник для переноса
│     ├─ tornode/              логика: мосты, torrc, control, HTTP-мост
│     ├─ tests/                34 теста + заглушка tor для интеграционных
│     └─ README.md             как пользоваться прототипом
└─ Release/                    выпуск 0.0.1: APK под телефон и эмулятор + PDF-мануал
```


## 🧭 Что делает

1. Принимает строки мостов из письма `bridges@torproject.org` — вместе с прозой письма,
   лишнее отсеивается само.
2. Поднимает подключение к сети Tor через эти мосты, показывая прогресс 0–100 %.
3. Отдаёт наружу локальный прокси: SOCKS5 и HTTP поверх него для программ,
   которые SOCKS5 не понимают.

Функционала браузера нет и не предполагается: ни окна просмотра, ни профилей,
ни защиты от отпечатков.

## 🎯 Платформы

Windows x64, Linux, macOS x86_64, macOS arm64, Android, iOS — правило 9.

На десктопе это локальный прокси. На Android и iOS ОС не даёт приложению обслуживать
чужой трафик через `127.0.0.1`, поэтому там то же ядро работает как VPN
(`VpnService` и `NEPacketTunnelProvider`). Подробности — в `Source/ЛОГИКА.md`.

## 📌 Состояние

**Версия 0.0.1 — первый работающий выпуск под Android.**

Проверено на эмуляторе Android 15: подключение через obfs4-мосты доходит до 100 %,
прокси поднимается, трафик выходит через сеть Tor (`check.torproject.org`
подтверждает `IsTor: true`). Работают все четыре плашки, смена цепочки и журнал.

- ✅ Ядро: разбор мостов, генерация `torrc`, control-протокол, запуск tor,
  obfs4 внутри процесса, фасад для интерфейса — 79 тестов.
- ✅ Приложение на Fyne: четыре плашки, сборка APK одной командой.
- ⬜ HTTP-прокси поверх SOCKS5 — многие приложения умеют только его.
- ⬜ Окно на компьютере — код готов, нужен компилятор C на машине сборки.
- ⬜ iOS — там нужен VPN-режим, это отдельная работа.

Сборка APK:

```bash
cd Source/app && ./build-android.sh arm64
```

Скрипт скачивает готовый tor от Guardian Project, вкладывает его в пакет и
подписывает. Готовый файл кладётся в `Release/`.

Как собрать и проверить ядро — `CONTRIBUTING.md`.

## ⚠️ О безопасности

Tor скрывает IP, но не делает анонимным: логины, куки и отпечаток браузера выдают
пользователя независимо от прокси. Для анонимного веб-сёрфинга нужен Tor Browser —
эта задача здесь не решается и не заявляется.

Прокси слушает только `127.0.0.1`. Выставлять его наружу нельзя: выход в Tor стал бы
открытым прокси для всего интернета.
